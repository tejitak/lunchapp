/**
 * Shop list
 */
(function(global, util) {
    "use strict";

    teji.getPackage("teji.lunch.listShops", Vue.extend({

        template: "#lunch-list-shops",

        data: {
        },

        created: function() {
        }
    }));
})(window, teji.lunch.util);