/**
 * main
 */
(function(global, $, util) {
    "use strict";

    global.main = Vue.extend({

        data: {
        },

        created: function() {
        },

        methods: {

        }
    });
})(window, jQuery, teji.lunch.util);