/**
 * Vote page
 */
(function(global) {
    "use strict";

    var Vue = require("vue");

    module.exports = Vue.extend({

        template: "#lunch-lp-tmpl",

        data: function(){ return {}; },

        created: function() {
        }
    });
})(window);