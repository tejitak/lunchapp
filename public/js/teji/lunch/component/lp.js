/**
 * Vote page
 */
(function(global) {
    "use strict";

    teji.getPackage("teji.lunch.component.lp", Vue.extend({

        template: "#lunch-lp-tmpl",

        data: function(){ return {}},

        created: function() {
        }
    }));
})(window);