/**
 * Vote page
 */
(function(global) {
    "use strict";

    var Vue = require("vue");

    module.exports = Vue.extend({

        template: "#lunch-voting-tmpl",

        data: function(){ return {}; },

        created: function() {
        }
    });
})(window);